using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Projectile : MonoBehaviour
{
    /*public void Launch(Vector3 hitPoint)
    {
        gameObject.GetComponent<Renderer>().material = GameObject.Find("Player").GetComponent<PlayerCharacter>().eye1.GetComponent<Renderer>().material;
        Rigidbody projectileRb = GetComponent<Rigidbody>();
        float projectileSpeed = 10f;
        projectileRb.AddForce(hitPoint * projectileSpeed, ForceMode.Impulse);
    }*/
}
